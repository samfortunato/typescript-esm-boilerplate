{
  "name": "typescript-to-esm",
  "version": "1.0.1",
  "description": "Project boilerplate for compiling TypeScript code into ECMAScript modules.",
  "main": "index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf ./dist && ./node_modules/.bin/tsc-watch --onSuccess \"sh ./scripts/convert-project-to-mjs.sh\"",
    "serve": "./node_modules/.bin/nodemon server.js"
  },
  "keywords": ["typescript", "esm", "module", "mjs"],
  "author": "Sam Fortunato",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^2.0.2",
    "tsc-watch": "^4.1.0",
    "typescript": "^3.7.5"
  }
}
